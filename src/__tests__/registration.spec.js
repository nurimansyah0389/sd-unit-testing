import Registration from '../lib/registration';

describe('Registration Unit Test', () => {

    it.todo('Throw an error if payload is empty');
    it.todo('Throw an error if email is invalid');
    it.todo('Throw an error if email exist');
    it.todo('Return success code if user has been registered successfully');

});